name: bump
on:
  workflow_dispatch:
    inputs:
      bumpAmount:
        description: Bump amount
        required: true
        default: patch
        type: choice
        options:
          - patch
          - minor
          - major
jobs:
  debug:
    uses: jaidlab/test/.github/workflows/callable/debug.yml@main
  job:
    name: ${{ github.workflow }}
    permissions:
      contents: write
    uses: jaidlab/test/.github/workflows/callable/bump.yml@main
    with:
      bumpAmount: ${{ inputs.bumpAmount }}
